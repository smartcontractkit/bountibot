import React, { useEffect } from 'react'
import * as firebase from 'firebase/app'
import 'firebase/auth'
// import Router from 'next/router'

const Logout = () => {
  // useEffect(() => {
  //   // firebase.auth().signOut()
  //   // Router.push('/')
  // })

  return <div>TODO - Logout</div>
}

export default Logout
