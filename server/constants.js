const rewardAmount = 100
const botName = 'bountibot'
const defaultLang = 'en'
const etherscanURL = 'https://ropsten.etherscan.io/'

module.exports = { rewardAmount, botName, defaultLang, etherscanURL }
